{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://brain.dev/schemas/config/inngest.json",
  "title": "InngestConfig",
  "description": "Inngest configuration - LOCAL ONLY. Inngest runs in local dev mode exclusively with no cloud connections.",
  "type": "object",
  "properties": {
    "dev": {
      "type": "boolean",
      "const": true,
      "default": true,
      "description": "Always true - local dev mode only"
    }
  },
  "required": ["dev"],
  "additionalProperties": false
}
