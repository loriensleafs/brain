# Brain MCP Server - Environment Configuration
# Copy this file to .env and customize as needed

# ==============================================================================
# BRAIN MCP SERVER CONFIGURATION
# ==============================================================================

# Transport mode: "stdio" for Claude/Cursor, "http" for TUI/web clients
# Default: stdio
# BRAIN_TRANSPORT=stdio

# HTTP server port (only used when BRAIN_TRANSPORT=http)
# Default: 8765
# BRAIN_HTTP_PORT=8765

# HTTP server host (only used when BRAIN_TRANSPORT=http)
# Default: 127.0.0.1
# BRAIN_HTTP_HOST=127.0.0.1

# Log file path (~ expands to home directory)
# Default: ~/.basic-memory/brain.log
# BRAIN_LOG_FILE=~/.basic-memory/brain.log

# Command to spawn basic-memory MCP server
# Default: basic-memory
# BRAIN_BM_CMD=basic-memory

# Log level: trace, debug, info, warn, error
# Default: info
# BRAIN_LOG_LEVEL=info

# Search guard mode: "warn" (log only), "enforce" (block duplicates), "off" (disabled)
# Default: warn
# BRAIN_SEARCH_GUARD=warn

# ==============================================================================
# OLLAMA CONFIGURATION
# ==============================================================================

# Ollama API base URL
# Default: http://localhost:11434
# OLLAMA_BASE_URL=http://localhost:11434

# Ollama request timeout in milliseconds
# Default: 60000 (60 seconds) - reduced from 10 minutes for fail-fast errors
# With batch API, embedding requests complete in <1 second
# OLLAMA_TIMEOUT=60000

# ==============================================================================
# INNGEST CONFIGURATION
# ==============================================================================
# Inngest runs in LOCAL DEV MODE only. No cloud connections.
#
# To use Inngest workflows:
# 1. Install: npm install -g inngest-cli
# 2. Start local dev server: npx inngest-cli@latest dev
# 3. Start Brain server: bun run start
#
# The Inngest dev server dashboard is available at http://localhost:8288

